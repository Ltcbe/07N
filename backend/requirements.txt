# --- Framework principal ---
fastapi==0.115.0
uvicorn[standard]==0.30.1

# --- ORM & Base de données ---
sqlalchemy==2.0.32
pymysql==1.1.1
alembic==1.13.2    # pour les migrations si tu en fais

# --- Tâches et worker ---
redis==5.0.8        # si tu utilises un broker Redis
rq==1.15.1          # exemple si tu utilises RQ pour les tâches
# ou bien, pour Celery : celery[redis]==5.4.0

# --- HTTP client (pour appeler l’API iRail) ---
httpx==0.27.0

# --- Validation et utilitaires ---
pydantic==2.8.2
python-dotenv==1.0.1

# --- Logging et outils ---
loguru==0.7.2
colorama==0.4.6

# --- Tests et outils de dev (optionnels) ---
pytest==8.3.2
pytest-asyncio==0.24.0
